# Maintenance Surface Map

This map keeps the maintenance layer simple. It tells Codex which existing
maintenance surface owns each kind of evidence or repair planning. It does not
create a new source of truth.

## Rules

- Use the smallest surface that owns the state being inspected.
- Prefer `snapshot` or `metrics` for orientation, `doctor` for diagnosis,
  `repair-plan` for proposed changes, and `validate` after changes.
- Do not run every doctor by default.
- Default command output is a bounded summary. Lists require a limit/cursor,
  metrics exclude raw payloads, and full logs/tool results are fetched only by
  explicit artifact or run reference.
- Successful receipts may stay compact, but failed receipts must inline
  decision-complete diagnostics: concrete causes, affected objects, severity,
  stable identifiers, and the next safe action. Keep the complete owner result
  available by artifact reference instead of forcing a second investigation.
- Aggregate health by declared severity and ownership scope, not only the
  owner's top-level `ok`. Advisory and external-dependency failures stay
  visible without becoming blockers. Reuse structured owner diagnostics and
  indexed query surfaces before opening raw logs; logs are last-mile evidence
  fetched by stable run, task, request, or artifact reference.
- Discover owner actions through the derived maintenance capability index;
  keep this Markdown map as the human source of truth and SQLite as its query
  projection.
- Do not let one maintenance surface mutate another module's production state.
- Use Hub only for tool continuity and bounded fallback, not as a permission
  shortcut.
- If a surface is missing a state or risk, update that surface rather than
  creating a parallel checker.

## Boundaries

| Surface | Owns | Does Not Own | Usual Entry |
| --- | --- | --- | --- |
| `workflow_orchestrator.py` | Task routing across memory, skills, slash templates, tool matrix, and validation choices; CLI validation returns bounded actionable failures plus a full-result reference while programmatic `validate()` retains the complete check set | Task execution, memory writes, business state writes, hiding failed child checks behind counts, or dumping every successful validation detail by default | `plan`, `doctor`, `validate [--full]`, `metrics` |
| `codex_rule_observer.py` | Fail-open `UserPromptSubmit`, `PostToolUse`, and `Stop` observations: soft task-fact hints, stable tool/reference facts, session-isolated conformance reporting, and closeout fact projection | `PreToolUse`, tool denial/rewriting, owner execution, permission changes, full prompt/tool-output storage, or claiming observed compliance as runtime enforcement | `hook <UserPromptSubmit|PostToolUse|Stop>`, `facts`, `validate`; regress with `python -m unittest _bridge\codex_rule_observer_tests.py` |
| `codex_environment_mirror.py` | Unified owner adapter for active-only Codex environment mirror status, planning, transactional control-plane freshness, Codex semantic contract review, local refresh, remote publish, semantic milestone release, validation, isolated restore planning/staging, retention, and Git/GitHub receipts; delegates snapshot implementation to `C:\Users\45543\codex-env-mirror\scripts\mirror_cli.py`. Generated current state binds `latest.json` to declared static hashes without rewriting stable contracts. Material changes nominate contracts for Codex review; a fingerprint-bound review receipt gates milestones. Routine publish is tag-free; explicit release is idempotent and verifies branch, annotated tag, public Release, and manifest attachment. The supported source chain is `WSL Work Git -> Windows bare Git -> validated mirror publish -> GitHub recovery repository`; live-source reads are read-only and block WSL runtime `apply`/reverse projection | Becoming a second snapshot authority, activating staged state, copying secrets/runtime databases into Git, machine-generating policy prose, rewriting stable contracts for cosmetic freshness, tagging every snapshot, reusing stale validation, mutating source files while reading live sources, reverse-projecting Windows-native state into Work Git, or bypassing refresh/publish/review/release/stage confirmation and remote readback | `status`, `plan`, `doctor`, `validate`, `refresh --confirm REFRESH-CODEX-MIRROR`, `publish --confirm PUBLISH-CODEX-MIRROR`, `release-plan`, `contract-review-plan`, `contract-review --decision <path=updated|compatible> --confirm REVIEW-CODEX-MIRROR-CONTRACTS`, `release --tag <seed-vX.Y.Z> --confirm RELEASE-CODEX-MIRROR`, `restore-plan --target-root <path>`, `stage --target-root <path> --confirm STAGE-RESTORE` |
| `wsl_workspace_owner.py` | Long-lived WSL workspace lifecycle owner. Maintains the sole daily Work Git authority, Windows bare Git backing store, Desktop's persisted UNC project identity, declared WSL execution targets, host compatibility projection boundary, validation-first bootstrap, handoff receipts, work-Git release readiness, and explicit cleanup plans. The former Windows workspace is a generated execution surface for Windows-only owners, never a source authority. Normal publication follows `WSL Work Git -> Windows bare Git -> validated mirror publish -> GitHub recovery repository` | Switching the default WSL distribution, importing ambient Windows Codex runtime/secrets/sessions/SQLite/caches/plugins into Work Git, publishing mirror releases, replacing the mirror owner, sharing writable runtime state, or managing Windows-only Office/GUI/browser/Weixin/OMEN/CC Switch sessions | `python _bridge\wsl_workspace_owner.py status|plan|validate|handoff|cleanup-plan|desktop-project-status|desktop-project-apply --confirm REGISTER-WSL-DESKTOP-PROJECT|mirror-export --kind work-git-release`; bootstrap requires `--confirm BOOTSTRAP-WSL-WORKSPACE`; unified facade operations are `workspace_status|workspace_plan|workspace_bootstrap|workspace_validate|workspace_handoff|workspace_cleanup_plan|workspace_work_git_release` |
| `intent_routing.py` | Shared read-only intent-signal primitives: normalized bounded matching, local negation suppression, evidence weighting, ranked candidates, and explicit routing diagnostics used by workflow, skill, resource, preflight, and maintainability owners | Domain policy, owner permissions, task execution, network/model calls, persistent state, or replacing owner-specific route tables | `validate`, `analyze --text <text> --terms <term...>` plus each caller's owner validator |
| `workflow_plan_detail.py` | Progressive `micro` / `standard` projections over the canonical full workflow plan; micro preserves minimal skill decisions plus compact owner/MCP/resource/network routing, while standard also preserves executable machine phases | Task classification, fallback decisions, owner policy, execution, state mutation, changing the canonical `full` plan, or removing decision fields solely to meet a size target | consumed by `workflow_orchestrator.py plan --detail micro|standard`; validate through workflow validation |
| `workflow_environment_context.py` | Read-only task-relevant projection of system layers, relevant member systems, maintenance entrypoints, MCP affinities, relationships, and expansion commands derived from system membership, the maintenance capability index, and MCP route index | Registering members, defining owner policy, copying complete inventories or contracts, choosing the task owner, executing commands, or becoming a second authority catalog | `build --message <task> [--domains ... --facts-json ...]`, `validate`; imported by `workflow_orchestrator.py` and carried through `execution_route_pack` |
| `bounded_output.py` | Shared recursive string/list/depth/byte budgets, explicit inline-output receipts, and the aggregate-validator projection that preserves failed child checks/issues/blockers, next actions, validation commands, and a stable full-result reference | Owner-specific semantic summaries, raw artifact storage, log retention, pagination backends, hiding required errors, or allowing aggregate counts to replace actionable child rows | imported by workflow/maintenance facades; validate through `maintenance_control_plane_tests.py` |
| `workflow_failure_diagnostics.py` | Shared read-only normalization of heterogeneous owner failures into bounded, decision-complete receipt diagnostics; preserves risk-first ordering, nested concrete items, stable identifiers, affected objects, and safe next actions while retaining the full owner artifact reference | Owner-specific repair decisions, raw log storage, success-payload expansion, permission changes, or replacing the complete owner result | imported by `workflow_owner_facade.py`; validate through `maintenance_control_plane_tests.py` and `workflow_owner_facade.py validate` |
| `workflow_action_synthesis.py` | Pure translation of reliable route-pack evidence and explicit fields into typed owner actions, deterministic defaults, and machine-readable `needs_input` | Owner execution, business state, retries, permission changes, or guessing ambiguous session tools | direct module validation; consumed by `workflow_owner_facade.py` |
| `workflow_owner_facade.py` | Versioned action/receipt contracts, owner capability declarations, resource/email/maintenance/MCP/mobile/network/Office adapters, bounded auto lifecycle, result consumption, classified execution affinity, `handoff_required` plus same-run result attachment, normalized lifecycle results, and lightweight run references | Central queues, workers, retries, permission escalation, arbitrary shell execution, duplicated business state, or treating call instructions as completion | `snapshot`, `validate`; invoked through `codex_workflow_entry.py run|status|wait|consume|cancel|attach-result` |
| `task_route_contract.py` | Explicit task-mode and business-owner precedence before keyword-scored evidence routes; emits a member-change admission/reconciliation gate for tools, commands, services, routes, schemas, providers, plugins, skills, workers, indexes, and related architecture members | General domain classification, task execution, owner state mutation, inferring a completed integration from prose, or replacing changed-file reconciliation | direct `validate`; `execution_route_pack.py` integration; workflow micro-plan regression |
| `execution_route_pack.py` | Canonical machine-readable execution route derived from the workflow plan, explicit task contract, resource gate, tool policies, and mandatory member-change gates; compact views are projected by `workflow_plan_detail.py` | Executing tools, mutating state, redefining owner-specific policy, or treating triggered gates as advisory | consumed by `workflow_orchestrator.py plan`; validate detailed semantics against `--detail full` and required gates against `--detail micro` |
| `codex_workflow_entry.py` + `workflow_finalization.py` + `workflow_closeout_package.py` | Unified lifecycle facade and closeout enforcement. It preserves owner-backed lifecycle operations and review cards, derives membership obligations from changed files, blocks false completion when membership evidence is missing, reports phase timings, and runs post-closeout mirror publication only after finalization plus all required self-update owner checks have succeeded | Business logic, central scheduling, permission escalation, replacing owner modules, treating run references as business truth, trusting task-text classification as the only member-change guard, publishing the mirror before source-affecting closeout owners finish or after a required owner check fails, or hiding blocker details during output compaction | `plan`, `run`, `status`, `wait`, `consume`, `cancel`, `attach-result`, `maintenance ...`, `preflight`, `checkpoint`, `closeout [--full-output]`, `review-summary`; `workflow_finalization_tests.py`; `workflow_closeout_package_tests.py` |
| `self_update_governance.py` | Read-only federated evolution coordination. It derives owner compatibility checks from `system_membership.py` while leaving full ambient health doctors available to global maintenance, adds focused skill/resource feedback checks, resolves affected systems from structured changed surfaces and changed-file impact, normalizes compatible receipt owner aliases, narrows broad membership-meta impact through validated authority plus specific changed-file closure, emits a stable dependency-aware change set, runs at most four independent read-only owner checks concurrently, and reports wall/slowest-owner timing | Applying repairs, becoming a central mutation bus, copying system or owner catalogs, treating owner transport success as task completion, replacing full health doctors with compatibility probes, narrowing an unvalidated or standalone membership-contract change, retrying below owner contracts, or letting task-text keywords override structured surfaces and changed-file evidence | `snapshot`, `doctor`, `validate`; optional `--changed-file`, `--changed-surface`, `--task-kind`, `--outcome`, `--config-changed`, `--major-change`; closeout integration through `codex_workflow_entry.py`; `self_update_governance_tests.py` |
| `maintenance_capability_registry.py` | Derived SQLite catalog over this maintenance map: system/module/action discovery, bounded queries, and safe capability resolution for the workflow facade | Business state, arbitrary shell execution, owner repair semantics, scheduling, or becoming a second source of truth | `build --apply`, `query`, `resolve`, `snapshot`, `doctor`, `validate`, `metrics`; facade: `codex_workflow_entry.py maintenance ...` |
| `local_mcp_hub_owner_mcp.py` | Explicit read-only profile/tool allowlist for Hub-first OpenAI Docs, Context7, Microsoft Docs, filesystem-admin/filesystem, MarkItDown, and MySkills calls through existing fresh stdio; compact owner receipts retain the tool result, route, error, and permission contract while removing duplicated initialize/command/stdout transport data | Write tools, current browser/GUI/mobile sessions, permission expansion, dynamic route discovery, or replacing owner result content with count-only summaries | imported by `local_mcp_hub.py`; `python _bridge\local_mcp_hub.py validate`; OpenAI protocol smoke: `python _bridge\mcp_session_doctor.py smoke --profile openai-docs` |
| `global_coherence_doctor.py` | Cross-surface read-only coherence checks plus severity-aware Owner health aggregation; failed Owners return bounded root cause, affected evidence, next action, elapsed time, and a full-result command reference | Repair, permission changes, replacing owner validators, executing proposals, mutating memory/tool/business state, or treating advisory/external dependency failures as blockers | `doctor`, `validate`, `metrics` |
| `system_membership.py` | Read-only system-member and architecture-evolution contracts. It defines pre-activation admission, post-change impact, closeout reconciliation, member identity, activation/reload boundaries, dependencies, structured execution/results, synchronized entry/exit surfaces, and negative retirement signals. Owner systems remain the authoritative inventories | Applying repairs, creating a second member database, mutating MCP/Codex config, treating registration/owner `ok`/a generated plan as completion, retaining decommissioned members as active entries, deleting isolated history, or replacing owner validators | `plan` before activation; `impact --changed` after files are known and again through finalization; `validate` plus `system_membership=ok` receipt at closeout; `snapshot`, `upgrade-plan`, `retirement-plan`, `repair-plan`, `retirement-signal`, `doctor` |
| `rule_governance.py` + `policies/rule_authority_registry.json` | Lightweight workflow-owned authority, lifecycle, and activation registry for rule-bearing surfaces. It records scope, owner, trigger, enforcement, validator, precedence, lifecycle, replacement, foreign/generated classification, legacy disposition, and a separate activation contract with effect (`mandatory`, `scoped_mandatory`, or `advisory`), layer role, runtime consumers, verification kind, and acceptance semantics. Mandatory rules are invalid when they have no executable consumer or are placed in an advisory layer | Replacing AGENTS discovery, duplicating system membership, executing owner policy, becoming a second workflow engine, copying rule bodies, treating advisory guidance as a permission/gate, or treating unregistered tests/history/generated files as active authority | `snapshot`, `plan`, `impact`, `retirement-plan`, `doctor`, `validate`; changed rule files require `rule_governance=ok` through `workflow_finalization.py`; route regressions must prove the runtime emits registered rule IDs rather than anonymous task-fact labels |
| `maintenance_upgrade_governance.py` | Read-only batch planner for system maintenance and upgrade work. It identifies owner systems, candidate modules, task-specific evidence needs, and phased batches while referencing existing tool policies and maintenance surfaces instead of redefining tool priority | Applying repairs, replacing `workflow_orchestrator.py`, duplicating MCP/SQLite/CodeGraph route policy, mutating state, or forcing every task through a fixed evidence checklist | `snapshot`, `plan`, `validate` |
| `online_access_gate.py` | Read-only direct-web access gate for online/external resource needs. It emits the resource-layer-first plan and validates that generic web is used only after resource-layer unavailability, configured owner/Hub route-chain exhaustion evidence, or explicit user direct-web instruction | Executing web/resource requests, hiding platform tools, mutating resource-layer state, replacing source-owning MCPs, or approving broad fallback without evidence | `plan`, `check`, `exception`, `validate` |
| `skill_active_catalog.py` + `skill_admission_discover.py` + `skill_orchestrator.py` + `skill_lifecycle_governance.py` + `skill_lifecycle_state.py` | Active configured-plugin resolution, authoritative admission/trust registry, stable skill identity across unambiguous moves, skill preflight, incremental user/system/plugin fingerprint refresh, persistent SQLite change/quality/lineage evidence, global-root/MySkills/plugin inventory fusion, runtime quarantine, collision review, scenario migration plans, approved archive actions, bounded default metadata manifests, and planned-vs-actual usage evidence. Batch validators refresh one routing context and reuse it for per-message scoring, while independent commands still refresh current state. | Plugin installation/update, a second approval authority in SQLite, automatic skill mutation/retirement, quality overriding domain or runtime eligibility, unapproved skill rewrites/deletion, plugin/system source mutation, task execution, stale cross-process routing caches, or bypassing MySkills for ordinary lifecycle writes | `skill_admission_discover.py`; `plan`, `refresh`, `lifecycle-state`, `audit-plan`, `repair-plan`, `record-lineage`, `apply-approved --confirm-apply`, `scenario-plan`, `record-usage`, `usage-summary`, `doctor`, `validate` |

Skill lifecycle notes:

- `metadata.codex.superseded_by` keeps legacy names resolvable while excluding them from active routing.
- Specialized domains may suppress broader domains when both match, for example `feishu` suppresses generic `docs_files`.
- User/plugin name overlap is resolved when the user skill owns the unqualified name and the plugin copy is exposed through a plugin-prefixed namespace.
- Long skill guides should keep a compact `SKILL.md` entry and move detailed material into `references/` for progressive loading.
- Lifecycle audit reports default entries over 500 lines separately from stale or broken skills, and detects byte-identical content published under different names.
- Domain routing may suppress a broader domain or a generic parent skill when a narrower owner is matched; candidate scores below 3 are treated as single-keyword noise.
- When `references/full-guide.md` exists, lifecycle validation requires the default entry to reference it; this prevents detached knowledge after progressive-disclosure refactors.
- Every skill plan and self-update pass runs a lightweight lifecycle refresh. New, removed, renamed, or modified skill trees are parsed and recorded in `_bridge/runtime/skill_lifecycle/skill_lifecycle.sqlite`; unchanged trees reuse their indexed record and do not create empty history events.
- The fingerprint covers the whole skill tree, not only `SKILL.md`, so changes to scripts, references, assets, and agent metadata enter the same governance path. Runtime environment availability is re-evaluated on every refresh even when files are unchanged.
- Plugin cache history is never an active capability source: the active catalog reads enabled plugin identities from Codex configuration, selects one current version per configured plugin, and excludes backup or staging paths.
- The lifecycle audit emits a bounded default metadata manifest. Non-default skills remain installed and are available through domain routing or explicit mention; the manifest must stay within the configured context budget.
- Generic routes select at most one execution skill and one routing, context, or constraint helper. Explicitly named multiple execution skills remain available.
- `_bridge/shared/skill-system/registry.json` owns admission and trust. SQLite stores only a derived query/evidence projection; `skill_orchestrator.py` owns routing.
- Only explicit `rejected` admission blocks routing. Existing `audit-pending`, deferred, and unregistered skills remain usable when their runtime contracts are valid.
- Quality evidence comes from bounded real-task events and can only break ties between equal business scores. It cannot override domain fit, required environment, implementation availability, or an explicit rejection.
- Stable `skill_id` follows a move only when source, name, and complete fingerprints produce exactly one match. Ambiguous relocations are assigned a new identity.
- Controlled evolution records `FIX`, `DERIVED`, or `CAPTURED` lineage with source, reason, version, and validation evidence. Recording lineage never authorizes automatic mutation.
- Legacy `skill_usage.jsonl` is retained as idempotent migration input; new quality events are written to SQLite and lifecycle usage windows query the index rather than rescanning JSONL.
| `codex_workflow_gate.py` | Memory preflight and finalization/iteration approval gating | Domain repair, tool routing, production writes | `memory-preflight`, `finalization-gate`, `validate` |
| `codex_config_guard.py` | Codex startup/config integrity plus separately reported provider-discovery and Desktop model-runtime compatibility diagnostics. It reports session-store state but delegates all automatic session compaction to the governed stopped-process pre-launch owner, avoiding competing writers and mismatched deadlines. Validation blocks only durable startup invariants while model/provider findings remain explicit advisory evidence | Treating dynamic active-workspace selection as a permanent invariant; requiring every provider to implement `/v1/models`; using an unreferenced catalog as current authority; forcing one reasoning-effort set on every model; killing or restarting the active Desktop session; applying session compaction from the config guard | `snapshot`, `doctor`, `repair-plan`, `validate`, `metrics`, `run-once` |
| `codex_desktop_environment_selection.py` + `wsl_codex_runtime.py` | Bidirectional Desktop environment selection reconciliation across the Windows host and isolated WSL app-server config, using last-synchronized state, routed backups, optimistic source checks, a single-writer lock, atomic writes, rollback, and full `[desktop]` table preservation before mode-specific MCP/session projection | Sharing the complete host config with WSL, selecting a provider/model, weakening WSL isolation, patching the signed Desktop package, blocking Codex startup after an owner failure, or treating projection success as proof that the restarted UI consumed it | `codex_desktop_environment_selection.py plan\|apply\|validate`; `wsl_codex_runtime.py plan\|apply\|validate`; automatic first-stage consumption by `codex_config_guard.py run-once --apply --phase pre-start-static`; regressions in `codex_desktop_environment_selection_tests.py`, `wsl_codex_runtime_projection_tests.py`, and `codex_startup_chain_tests.py` |
| `codex_config_projection.py` | Field-ownership-aware, secret-free projection of live non-provider Codex settings. The sole recovery ledger is `~/.codex/state/managed-config-projection.json`; active values are adopted, missing managed values are restored, explicit live values win conflicts, and known Desktop preferences use native setting readback/sync. CC Switch's current provider record is the sole model-catalog authority: `model_catalog_json` is never stored in the recovery ledger, providers without an active `settings_config.modelCatalog` remove stale catalog pointers, and providers with a catalog keep the live CC Switch projection. Retired `common_config_codex`, `codex_managed_projection_v1`, and provider `commonConfigEnabled` surfaces are removed and must remain absent | Selecting providers, copying auth/token material, recreating CC Switch common configuration, owning startup-baseline invariants, preserving provider-derived catalog pointers across provider changes, silently deleting managed keys, directly editing Desktop global-state JSON, or overwriting explicit live values | `snapshot`, `doctor`, `plan`, `apply`, `validate`; guarded `apply --reseed-managed --confirm-reseed RESEED-MANAGED`; `codex_config_projection_tests.py`; automatic consumption by config guard and provider watcher |
| `codex_plugin_runtime_doctor.py` | Bounded read-only classification of Codex Desktop plugin/runtime evidence: external app-server interrupts, native-addon initialization failures, remote catalog authentication limits, and rejected plugin metadata; preserves evidence references and package/version ownership | Patching signed `WindowsApps` content, replacing native addons, disabling unrelated plugins, treating the last stderr line as an exit cause, or making upstream optional-capability failures block startup | `snapshot`, `doctor`, `validate`; focused regression via `python _bridge\codex_plugin_runtime_doctor_tests.py` |
| `shared/process_liveness.py` | Side-effect-free cross-platform PID liveness and process-creation identity checks shared by startup, launcher, MCP, mirror, network lease, and background-refresh owners; creation identity distinguishes stale-lock PID reuse without signalling the process; includes a first-party unsafe-probe scanner | Process termination, process-tree cleanup, ownership decisions, application health checks, or probing protected processes beyond limited query rights | `python _bridge\shared\process_liveness_tests.py`; `python _bridge\codex_plugin_runtime_doctor.py validate` |
| `codex_session_store_doctor.py` | Codex local session-store restore-performance evidence and compression-only restart-boundary maintenance: bounded `.codex\sessions` sizing, largest/recent JSONL files, guarded in-place JSONL compaction of tool outputs/events/reasoning blobs, backup manifests, and metrics for restart/resume risk. It does not create continuation tasks, move sessions, archive sessions, or delete sessions. Automatic maintenance only runs while the Codex process family is stopped and only targets sessions at or above the risk threshold. Its non-blocking owner lock records PID plus process-creation identity, replaces only old locks proven dead or PID-reused, and keeps legacy or unverifiable live-owner locks fail-safe. | Creating continuation tasks for storage maintenance; moving, deleting, or archiving session files; hot-editing while Codex is running; compacting conversation messages or existing `compacted` context blocks; disabling MCPs; lowering model/tool capability; changing network/model/config routes | `snapshot`, `doctor`, `repair-plan`, `compact-plan`, `compact --confirm-compact`, `auto-maintain --apply`, `metrics`, `validate` |
| `codex_prelaunch_maintenance.py` | Bounded fail-open adapter used by the governed Codex Desktop launcher after the stopped-process boundary: starts `codex_session_store_doctor.py auto-maintain --apply --boundary pre-launch` without a console, enforces a finite timeout, consumes its JSON receipt, and permits Desktop startup after owner failure or timeout. | Selecting sessions, changing compaction thresholds, editing session files directly, launching Desktop, treating maintenance failure as a startup blocker, or replacing the session-store owner | `run --workspace <workspace> --timeout-seconds <seconds>`, `validate`; regression via `python -m unittest _bridge\codex_prelaunch_maintenance_tests.py` |
| `codex_runtime_cache_governance.py` | Cached dependency-runtime inventory, current/rollback retention, stale install-residue detection, and reversible manifest-backed quarantine | Deleting cached runtimes, moving the active runtime, changing providers/models, or touching Desktop/CLI package files | `snapshot`, `doctor`, `repair-plan`, `quarantine --apply --confirm QUARANTINE-OLD-RUNTIMES`, `validate` |
| `memory_router.py` | Task-fit routing across memory layers: current context, quick pass, user profile, PMB, external knowledge, record-store evidence, and one-shot work notes | Reading/writing PMB content, absorbing notes, executing business work, treating memory as live state proof, or granting authorization from work notes | `route`, `validate` |
| `mcp_session_doctor.py` | MCP config/protocol/current-turn evidence and gateway summary | Business module health, memory content quality, file backup policy | `validate`, `metrics` |
| `mcp_session_doctor_routes.py` | Read-only CLI command dispatch helper for MCP session diagnostics; maps CLI command names to injected `mcp_session_doctor.py` handlers while preserving payload shaping | Observation storage, protocol smoke execution, gateway calls, route policy, permission/fallback semantics, or business diagnostics | consumed by `mcp_session_doctor.py`; validate with `mcp_session_doctor.py validate` and `local_mcp_hub.py validate` |
| `mcp_route_policy.py` | Classified `hub_first` / `session_native_first` / `owner_cli_first` precedence, required first step, same-boundary fallback ordering, and cross-surface stale-guidance checks | Tool execution, permission expansion, owner business state, or treating generic skill prose as stronger than a profile affinity | direct module validation; `mcp_capability_routes.py validate` |
| `mcp_profile_launcher.py` + `mcp_profile_launcher_process.py` / `mcp_lazy_stdio_proxy.py` | Windows-hidden MCP launch routing plus command-fingerprinted catalog caching for heavy stateful profiles. Lazy profiles enter the proxy loop in the launcher process instead of retaining a second waiting Python parent; Desktop still gets one lightweight task-bound stdio proxy and the guarded child still starts only on the first non-catalog call. Non-lazy profiles keep the existing guarded subprocess path. | Sharing a stdio child across tasks, bypassing target permissions, treating cached schemas as runtime health, changing non-lazy profile process semantics, or replacing `mcp_launch_guard.py` cleanup/supervision | `python _bridge\mcp_lazy_stdio_proxy_tests.py`; refresh with `python _bridge\mcp_profile_launcher.py <cdev|pw|gui|drawio> --warm-cache`; real-child smoke via `mcp_session_doctor.py protocol-smoke <profile>` |
| `local_mcp_hub.py` + `local_mcp_hub_pmb_runtime.py` | Classified Hub-first entrypoints, low-risk same-boundary gateway continuity, and in-process PMB recovery singleflight so simultaneous restored tasks share one daemon recovery | Owning-tool permissions, session-bound native identity, cross-process PMB daemon ownership, or a global replacement for affinity selection | `validate`, `smoke`; `python _bridge\local_mcp_hub_pmb_tests.py` |
| `local_mcp_hub_resource_search.py` | Read-only bounded text, image, news, video, book, and explicit-URL extraction tools for the resource layer's `generic_search` owner; project-local DDGS health, backend selection, per-request proxy use, UTF-8 worker output, and result shaping | Resource intent/routing, downloads, installs, persistent writes, global proxy/DNS changes, or direct-Web permission decisions | Hub `resource_search.health|validate|text|images|news|videos|books|extract`; `python _bridge\local_mcp_hub.py validate`; `python _bridge\resource_fetcher_tests.py` |
| `local_mcp_hub_network_gateway.py` | Thin Hub adapter for `network_gateway.*` tools; translates Hub arguments into bounded `codex_network_gateway.py` CLI calls | Network route policy, probing implementation, system proxy/DNS changes, Clash config edits, Hub tool registration, or resource fetching | Hub `network_gateway.snapshot|plan|env|smoke|lease_start|lease_status|lease_stop|lease_cleanup|validate`; `local_mcp_hub.py validate` |
| `mcp_capability_routes.py` | Machine-first derived MCP route index, identity-weighted lookup, explicit browser-session coverage, and affinity-ordered fallback chains for Codex/tool orchestration | Human-facing explanation, replacing the Markdown matrix source of truth, permission changes, or scoring generic policy prose as capability identity | `build`, `lookup`, `validate` |
| `network_policy.py` / `network_doctor.py` | Read-only per-target network route discovery, proxy/DNS/path evidence, route recommendation, target probing, and per-process environment suggestions for Codex/MCP/resource/package/browser work | Permanent system proxy/DNS changes, Clash/Mihomo node switching, forcing all traffic through one endpoint, or bypassing owner-tool permissions | `network_doctor.py snapshot`, `recommend`, `env`, `probe`, `validate`; Hub `network.*` |
| `codex_network_gateway.py` | Thin Codex network gateway control plane for resource/tool/package/GitHub/docs/browser/paper/image/dataset/web callers: target classification, optional probe-selected direct/proxy route, per-process environment, isolated Mihomo/proxy-chain lease commands, route evidence, `owner_tool` + profile-versioned route-cache stats, and structured degraded warnings for optional Clash control surfaces | Long-running daemon ownership, permanent system proxy/DNS changes, Clash subscription/config edits, Codex conversation route mutation, credential handling, owner-tool permission changes, resource-source selection, or replacing resource/GitHub/browser/package tools | `snapshot`, `interfaces`, `plan --probe`, `env`, `smoke`, `lease-start`, `lease-status`, `lease-stop`, `lease-cleanup`, `validate` |
| `clash_node_metrics.py` | Clash/Mihomo node evidence cache and request-aware node recommendation: bounded high-frequency delay sampling, lower-frequency isolated real-access probes for delay-qualified candidates, freshness policy, access-score-aware ranking, and compact recommendation payloads for the Codex network gateway | Main Clash node switching, system proxy/DNS changes, Clash subscription/config edits, permanent routing decisions, resource fetching, daemon scheduling, or broad all-node real-access scans | `snapshot`, `refresh-delay`, `refresh-access`, `recommend --refresh-if-stale`, `validate` |
| `network_gateway_leases.py` | Short-lived isolated Mihomo proxy lease lifecycle: start localhost-only per-request proxy, return per-process env, inspect status, stop/archive, cleanup expired/dead leases | Main Clash node switching, system proxy/DNS changes, Clash subscription/config edits, Codex conversation routing, daemon ownership, or returning controller secrets | `start-isolated`, `status`, `stop`, `cleanup`, `validate` |
| `network_gateway_component_lab.py` | Lab-only absorption and smoke testing of proxy gateway components such as proxy-chain, GOST, easy_proxies, and Resin; localhost proxy wrapper tests; local runtime package install under `_bridge/runtime/network_gateway_lab` | Production route ownership, permanent network changes, system proxy/DNS edits, Clash subscription/config edits, Hub startup changes, Codex conversation routing, or broad proxy pool deployment | `snapshot`, `component-plan`, `proxy-chain-smoke`, `gost-plan`, `validate` |
| `resource_broker.py` / `resource_store.py` / `resource_scheduler.py` | Single- and multi-request orchestration, structured batch-item identity, independent routing and acceptance, bounded concurrency, local `resource_cli` attempts, owner-tool/package-manager handoff tracking, representative gateway targets for target-only owner requests, owner result attachment, progress events, attempt records, persisted manifests, preview/metadata paths, request index, and receipts for Codex consumption | Bypassing MCP permissions, merging independent targets into one natural-language query, treating command success as resource acceptance, destructive installs without risk judgement, browser/GitHub/docs execution inside the broker process, deciding proxy/node policy, or durable permission changes | `request`, `request-batch`, `status`, `attach-result`; via `resource_cli.py request|request-batch|batch-status|status|attach-result` or Hub `resource.request|resource.request_batch|resource.status|resource.attach_result` |
| `resource_execution_budget.py` with broker, owner, source, and scheduler consumers | One monotonic total deadline for each resource request or batch, remaining-time propagation to downstream phases, control-plane execution reserve, and terminal `total_budget_exhausted` evidence for work that must not start | Per-phase timeout resets, unlimited retries, network-route policy, silently raising caller budgets, or treating timeout exhaustion as a successful empty result | `python _bridge\resource_execution_budget.py`; behavioral coverage in `python _bridge\resource_fetcher_tests.py` |
| `resource_owner_tool_registry.py` / `resource_owner_executor.py` / `resource_youtube_feed_owner.py` / `resource_node_package_owner.py` | Canonical resource owner names and bounded owner execution. The GitHub adapter consumes structured operations for repository/code/issue/PR search and metadata/README/tree/file/release reads, runs Hub-first under one total budget, and returns phase and deliverable evidence. Other adapters retain their scoped metadata/content behavior | Route ordering, permission expansion, remote writes, cloning/install side effects, generic YouTube search, transcript/media download, global npm installs, implicit approval, or direct web fallback policy | `python _bridge\resource_owner_executor.py validate`; `python _bridge\resource_node_package_owner.py validate`; `python _bridge\resource_youtube_feed_owner_tests.py`; full resource and complex GitHub coverage in `resource_fetcher_tests.py` |
| `structured_task_envelope.py` / `resource_cli.py custom` | Versioned machine-readable resource delegation for action, target, kind, quantity, uniqueness, source coverage, freshness, transfer, package, materialization, quality, safety, and source execution. `resource.execution` carries operations, selectors, deliverables, ordered phases, limits, output depth, and acceptance; required phases/deliverables block metadata-only false completion. CLI and Hub/MCP callers share the existing broker/job/receipt implementation | A second queue or retry engine, natural-language permission inference, silently completing unsupported phases, owner-tool permission expansion, network route selection, or replacing legacy commands before compatibility validation | `python _bridge\structured_task_envelope.py`; `python _bridge\structured_task_envelope_tests.py`; `python _bridge\resource_cli.py custom --request-json <json> --json`; complex phase regressions in `resource_fetcher_tests.py` |
| `resource_download_backends.py` | Optional process-backed URL materialization backends for resumable downloads: curl and aria2c command construction, proxy/no-proxy env consumption, timeout handling, checksum metadata, and backend availability reporting | Installing aria2c/curl, running aria2 RPC daemons, changing system proxy/DNS, selecting global network routes, or bypassing resource policy/owner permissions | `validate`; consumed by `resource_fetcher.py` when request metadata sets `download_backend` or `resume_download` |
| `resource_windows_package_manager.py` | Windows package-manager package owner adapter: Chocolatey/winget availability, bounded search, install planning, explicitly approved install execution, and verify command metadata for resource package requests | Silent installs, uninstall/upgrade workflows, global proxy/DNS or package-manager configuration changes, permission escalation, or replacing package policy/risk review | `availability`, `validate`; consumed by `resource_package_owner.py` for `windows_tool`/`choco`/`winget` ecosystems |
| `windows_kernel_pool_diagnostics.py` with `windows_kernel_pool_governance.py` and `windows_kernel_pool_remediation.py` | Microsoft-signed PoolMon capture, compact success summaries, failure evidence retention, boot-segmented SQLite trends, bounded periodic monitoring, WPN recovery guards, and fingerprint-locked reversible stale local-firewall-rule cleanup | Unconditionally restarting services, changing drivers, disabling firewall/notifications, broad firewall cleanup, changing page-file or memory policy, deleting failure evidence, treating aggregate Pool Tags as unique driver attribution, or combining built-in GPU and Pool WPR profiles in unbounded file mode | `capture --label <label> [--summary-only]`, `index-rebuild`, `doctor`, `metrics`, `schedule-plan`, guarded `schedule-apply --confirm INSTALL-KERNEL-POOL-MONITOR`, `wfp-plan`, fingerprint-locked `wfp-apply`, governed `wfp-rollback`, `wpn-plan`, guarded `wpn-recover --confirm RESTART-WPN-HANDLE-LEAK`, and `validate`; when ETW is necessary use one memory-mode/custom-buffer profile with an explicit duration and output-size budget; validate with the three `windows_kernel_pool_*_tests.py` suites and `code_maintainability.py validate` |
| `windows_memory_governance.py` | Unified, bounded memory diagnosis across physical availability, system commit, page-file capacity/current/peak use, paging activity, cache/standby, optional compression-counter capability, paged/nonpaged pools, service-attributed `svchost` families, process role/family working set, private commit, private-to-resident gaps, handles, process counts, MCP lifecycle summary, and specialized kernel-pool SQLite evidence. SQLite trends compare only samples from the same boot and classifier version, so restarts and taxonomy upgrades cannot masquerade as growth. It owns cross-layer attribution and routing, while periodic capture is registered in the unified scheduler. | Killing ordinary applications, clearing standby/cache or trimming working sets as a substitute for source attribution, disabling Defender/services, changing drivers/page-file policy from one sample, applying firewall changes, duplicating MCP or kernel-pool repair authority, persisting raw command-line process dumps, treating hard faults as page-file faults without correlation, comparing incompatible trend segments, or applying Job Object limits to arbitrary/shared processes | `capture`, `metrics`, `trends [--hours N] [--category name]`, `doctor`, `repair-plan`, `validate`; specialized actions continue through `resource_process_doctor.py`, `local_pmb_memory.py`, and `windows_kernel_pool_diagnostics.py`; validate with `windows_memory_governance_tests.py` and unified scheduler `task-drift` |
| `windows_hardware_owner.py` | Read-only all-device Windows PnP inventory and diagnostics. Fast snapshots use bounded CIM entity and signed-driver tables for class, status, problem code, service, manufacturer, and driver visibility; exact `device` queries own the more expensive parent/children topology, IDs, location, bus, removal-policy, and identity fingerprint evidence. It also provides class/problem reports, fixed hardware event sources, and snapshot diff. | Any device, driver, service, firmware, storage, registry, power-policy, or operating-system mutation; generic PowerShell/shell/PnPUtil/DeviceIoControl/WinUSB/HID/serial/smart-card/vendor execution; resident watchers, startup entries, services, drivers, packages, or MCP activation | `snapshot [--full]`, `device --instance-id <exact-id>`, `problems`, `classes`, `events --hours <N> --limit <N>`, `diff --before <json> --after <json>`, `doctor`, `validate`; regression via `python -m unittest _bridge\windows_hardware_owner_tests.py` |
| `usb_device_owner.py` | Read-only Windows USB inventory and diagnostics: PnP topology and stable identity, signed-driver metadata, USB storage mapping, fixed USB event logs, bounded in-process hot-plug observation, isolated pyserial/PyUSB+libusb/hidapi/FIDO2/pyscard discovery, and Android ADB/Fastboot status without starting an ADB server. Its `storage` result is the hardware identity and health handoff for content owners; it does not grant device-control permission. | Device/driver/firmware/storage/service/policy mutation, arbitrary PowerShell or shell execution, ADB shell/push/install/reboot/sideload, Fastboot flash/erase/reboot, resident watchers, startup registration, or native/MCP activation | `snapshot [--full]`, `storage --drive-letter <letter>`, `doctor`, `events --hours <N> --limit <N>`, `watch --duration <seconds> --interval <seconds>`, `diff --before <json> --after <json>`, `android`, `validate`; regression via `python -m unittest _bridge\usb_device_owner_tests.py` |
| `usb_device_control.py` | Guarded Windows USB control plans and direct-owner execution: global PnP rescan, exact-fingerprint restart of allowlisted noncritical leaves, tightly allowlisted disable, verified child topology and protected USB interface-class guards, cross-session operation serialization, durable pending receipts before mutation, bounded post-state observation, identity-bound enable recovery, and lock-consumed receipt-authorized rollback | Workflow-facade mutation, core USB/host/root/composite/input/storage/network/Bluetooth control, unknown/non-leaf topology, device removal/eject, driver install/remove, firmware, format/storage writes, policy changes, `/force`, `/reboot`, arbitrary PnPUtil/PowerShell/shell, ADB, or Fastboot | Read-only `plan`, `status`, `doctor`, `validate`; direct explicit `apply --action <rescan, restart, or disable> ... --confirm <exact-token>` and `rollback --operation-id <id> --confirm ROLLBACK-USB:<id>`; regression via `python -m unittest _bridge\usb_device_control_tests.py` |
| `music_library_owner.py` with `music_library_planner.py` and `music_library_transaction.py` | Reusable local music-library inventory, reviewed correction consumption, artist/album/track layout planning, lyrics and artwork association, duplicate/conflict quarantine, USB storage identity binding, exact-plan confirmation, same-volume non-overwriting moves, durable journals, interruption recovery, rollback, and full SHA-256 post-state validation. It reuses `usb_device_owner.py storage` for hardware admission and `ffprobe`/the existing audio toolkit stack for media inspection. | Network research, arbitrary correction fields or target paths, device control, format/eject/partition operations, deletion, overwrite, cross-volume copy, transcoding, tag rewriting, or accepting a transport/move result without hash validation | Read-only `inventory --root <path>`, `doctor [--root <path>]`, `validate [--plan <json> --expected source|applied]`; plan `plan --root <path> --corrections <json>`; explicit `apply --plan <json> --confirm-plan-id <id>` and `rollback --plan <json> --confirm-plan-id <id>`; regression via `python -m unittest _bridge\music_library_owner_tests.py` |
| `audio_toolkit/audio_toolkit.py` and `audio_toolkit_gui.py` | Existing audio stream/tag inspection, analysis, conversion, trimming, normalization, silence detection, Chinese transcription, lyric drafting/alignment, and direct-use GUI. Music-library organization should reuse its installed FFmpeg/ffprobe and media knowledge instead of creating a parallel media stack. | Music-library naming authority, USB device admission/control, implicit transcription or transcoding, destructive source replacement, or network metadata research | `audio_toolkit.py --help`; operation-specific commands documented by `windows-audio-ops`; GUI only when the user needs interactive control |
| `resource_python_package_installer.py` | Explicitly approved Python package installation into an isolated pip `--target` directory, per-request network environment consumption, timeout handling, and installed-distribution version verification | Global/site-packages installs, PATH or pip configuration changes, uninstall/upgrade policy, metadata discovery, implicit approval, or selecting package dependencies | `validate()` through `resource_package_owner.py`; full coverage in `resource_fetcher_tests.py`; live proof through a bounded `resource_cli.py job run --intent package_dependency --package-action install --install-approved --allow-filesystem-write --target-dir <isolated-path>` request |
| `cloakbrowser_owner.py` | Explicit-use CloakBrowser planning and diagnostics, isolated wrapper discovery, and separate browser-binary acquisition requirements with provenance and hash evidence | Replacing normal Chrome/Playwright/DevTools routes, implicit patched-browser download, normal profile/cookie reuse, permission inference, or global proxy mutation | `snapshot`, `doctor`, `plan --task <task> [--authorized]`, `validate` |
| `resource_network_execution.py` | Resource-layer adapter from `codex_network_gateway.py` plans to executable env/unset packages, URL attempt specs, and owner-tool handoff network guidance | Network policy decisions, resource semantics, starting live leases, mutating system proxy/DNS, bypassing owner MCP permissions, or executing package/GitHub/docs/browser tools | `validate`; consumed by `resource_fetcher.py` and `resource_broker.py` |
| `resource_process_doctor.py` with `resource_process_reporting.py` | Resource/MCP process fanout, stale launch batches, current-session evidence mapping, bounded diagnostic/cleanup receipts, and safe owner cleanup. Observed totals remain visible, while cleanup and memory-pressure issues use age-qualified persistent roots/process working set; short startup waves are recorded as transient and do not create cleanup review items. | Current-turn tool callability, bridge queues, ordinary application memory policy, fixed-PID cleanup, or treating a single launch wave as a persistent leak | `doctor`, `metrics`, `repair-plan`, `cleanup [--safe-apply --apply]`, `validate`; current coverage includes session-bound `desktop-weixin` and other declared MCP profiles; fanout defaults to a two-minute evidence gate before it becomes actionable |
| `resource_process_observations.py` | Read-only current-turn MCP observation log parsing, freshness filtering, and profile-to-process-group evidence mapping | Process snapshot collection, process cleanup, repair planning, permission decisions, or route fallback execution | consumed by `resource_process_doctor.py`; validate through `resource_process_doctor.py doctor\|metrics\|validate` |
| `popup_window_doctor.py` | Codex/MCP console popup attribution for PowerShell/cmd/conhost parent chains | Disabling tools, killing processes, changing MCP capability, rewriting Codex app behavior | `snapshot`, `doctor`, `validate`, `metrics`, `observe` |
| `cli_anything_governance.py` | CLI-Anything / cli-hub installation health, catalog discovery, local installed-harness discovery and required command surfaces, Codex skill completeness, trusted-project policy | Automatically installing harnesses, running target software during read-only validation, bypassing package-manager review, MCP configuration | `snapshot`, `doctor`, `validate`, `metrics`, `search`, `info`, `installed`, `commands` |
| `cli_anything_microsoft_office/agent-harness` | Bounded native Word, Excel, and PowerPoint create/inspect/edit/export operations, versioned operation allowlists, transactional output, per-operation receipts, and real Office E2E | Arbitrary COM, VBA/macros, visible-session reuse, OOXML-only content planning, central queues, or document state duplication | `cli-anything-microsoft-office --json system status`; app `operations|inspect|edit`; focused pytest and opt-in real Office E2E |
| Memory absorption candidates | Evaluating external memory/knowledge projects such as `agent-memory-engine`, `ArcRift`, and `localmem` as pattern sources for local-first evidence, source attribution, lifecycle state, deduplication, temporal/drift handling, PMB recall, work notes, and external knowledge capture | Installing another memory server by default, bypassing note/PMB approval, writing long-term memory directly, storing secrets, or treating external project state as local truth | `memory_governance.py absorb-plan`, `pmb-organize-plan`, `recall-verify`, `validate`; `external_knowledge.py doctor` |
| Developer toolchain utilities | Fast utility support for owning workflows: `rg` search, `fd` discovery, `uv` Python tool/env execution, `uvx` one-shot Python CLI execution, `ruff check` targeted Python lint feedback, and Playwright browser evidence | Becoming a new governance layer, package installation without explicit task intent, broad formatting churn, replacing owning module validators, or hiding dependency upgrades in unrelated work | `code_maintainability.py toolchain` plus the owning module's `validate`/doctor/readback |
| Windows generic desktop automation candidates | Evaluating reusable capability patterns from external projects such as `CursorTouch/Windows-MCP`: screenshot-first inspection, accessibility snapshots, `WaitFor`, tool filtering, local-only transport, and no-flash/no-telemetry defaults | Installing the whole project by default, bypassing existing GUI/Weixin/browser owners, enabling unrestricted PowerShell/FileSystem/Registry/Process tools, or claiming native availability without current-turn evidence | `_bridge/docs/mcp_capability_matrix.md` plus `mcp_capability_routes.py lookup --terms windows-mcp screenshot waitfor` |
| `backup_hygiene_doctor.py` | Backup placement, scattered backup detection, dry-run migration plans, controlled archive-only apply for eligible old backups | Source file semantics, business state correctness, deletion, compression, or source rewrite | `doctor`, `repair-plan`, `apply --confirm archive-old-backups`, `validate` |
| `memory_governance.py` | Candidate notes, approved note absorption/indexing, post-apply recall checks, read-only recall verification, PMB organization plans, PMB fact review markers, reversible empty-workspace retirement with negative tombstones, memory absorb/stale proposals, one-shot work-note append/read/clear | Direct task execution, authorization inheritance from work notes, skill body rewrites, secrets storage, work-note interpretation, deleting or rewriting PMB event history, retiring active or non-empty PMB workspaces | `doctor`, `review-summary`, `apply-approved`, `recall-checks`, `recall-verify`, `work-note-add`, `work-note-read`, `work-note-clear`, `absorb-plan`, `pmb-organize-plan`, `pmb-fact-repair-plan`, `pmb-fact-apply-approved`, `pmb-workspace-retire-plan`, `pmb-workspace-retire-apply --confirm-apply`, `validate` |
| `workflow_review_queue.py` / `workflow_iteration_owner.py` / `memory_iteration_owner.py` | Persistent candidate review state, exact identity and guarded lifecycle transitions, owner allowlisting, single-candidate compatibility, and atomic reviewed-memory batches with one routed backup, one index write, per-candidate readback, and resolved receipts | Deriving approval, silently dropping pending candidates, treating bounded closeout card output as a complete backlog view, arbitrary candidate commands or destination paths, bypassing privacy/identity checks, direct PMB writes, or resolving before owner validation | Queue `snapshot`, `get`, `transition`, `dispose`, `validate`; owner `plan`, `apply`, `validate`, `resolve`, `recall --review-id <id> [--review-id <id> ...] [--confirm-apply]`; memory validation through `python _bridge\memory_governance.py validate`; regression via `python -m unittest _bridge\workflow_iteration_owner_tests.py _bridge\workflow_iteration_e2e_tests.py` |
| `draft_governance.py` | Draft artifact metadata, two-axis content/workflow state, draft index consistency, and retained-reference validation | Pending decision ownership, closeout queue creation, approval execution, active incident tracking, or inferring state from file names | `snapshot`, `validate` |
| `local_pmb_memory.py` + `local_pmb_memory_process.py` + `pmb_compatibility.py` | PMB daemon health, hidden Windows command/daemon launch, cross-process daemon lifecycle serialization, on-demand warm-daemon recovery after configured idle exit, PMB migration/cutover, read-only PMB work entrypoints with configured `PMB_HOME`/workspace, package compatibility drift, and SQLite/LanceDB/BM25 index consistency | Long-term memory approval, unsafe PMB event rewrites, blind third-party package patching after source drift, permanent heavy in-process fallback servers, disabling idle exit merely to hide recovery defects, visible-console fallback when `pythonw.exe` is missing, or replacing the configured Hub-first PMB route | Hub `pmb.*` tools first; `pmb-prepare`, `pmb-recall`, `daemon-status`, `daemon-ensure`, `snapshot`, `doctor`, `repair-plan`, `pmb-compat-doctor`, `pmb-compat-repair-plan`, `pmb-compat-apply --apply`, `validate`, `metrics`; `python _bridge\local_pmb_memory_process_tests.py` |
| `memory_candidate_notes.py` | Read-only ad hoc candidate-note scanning and draft absorption plan construction for memory governance review cards | Approving memories, writing absorption indexes, archiving source notes, PMB/user-profile writes, or deciding final long-term destinations without review | consumed by `memory_governance.py candidate_notes|absorb_plan`; validate through `memory_governance.py validate` |
| `slash_command_governance.py` | Governed slash template proposals, registry validation, routed-backup apply, and render-smoke checks | Executing rendered templates, bypassing approval, replacing the read-only/render-only custom slash MCP | `snapshot`, `proposal`, `validate`, `apply --confirm-apply`, `render-smoke` |
| `tool_coordination.py` | Slash/scratch coordination metadata and registry boundary checks | Production DB authority, shell execution, bridge repair | `snapshot`, `validate` |
| `code_maintainability.py` | Code maintainability metrics, refactor target planning, module-context routing, developer toolchain status, the derived module capability index at `_bridge/runtime/module_capability_index.json`, and the categorized module asset catalog at `_bridge/runtime/module_asset_catalog.json`. It helps Codex find reusable modules before creating new code and supports two lookup modes: maintenance tasks emphasize owning system, responsibility, state behavior, owner CLI, and validator; code tasks emphasize role, applicable scenario, reuse target, public entrypoints, and capability terms. `build-module-index --limit` limits only response samples and never truncates the persisted index. Local coding rules are documented in `_bridge/docs/code_maintainability_guidelines.md`. | Formatting, automatic refactors, semantic correctness, deleting backup files, applying refactors without separate approval, treating derived runtime indexes as source of truth, or using categories as permission boundaries | `snapshot`, `toolchain`, `module-context`, `module-assets --task-mode maintenance|code`, `build-module-index`, `lookup-module`, `plan`, `validate` |
| `mobile_openclaw_bridge/source_scan.py` | Source-only mobile bridge file inventory for governance scans. It excludes runtime profiles, attachments, logs, DB/WAL files, and backups so code analysis does not drown in bridge data. | Queue inspection, repair, file deletion, attachment analysis, runtime state mutation | `snapshot`, CLI `source-scan`, `source-scan --validate` |
| `mobile_openclaw_bridge/dashboard_state.py` | Read-only dashboard state payload assembly for `/api/state` and task detail views: task rows, user summaries, events, runtime display data, and status counts | HTTP routing, dashboard actions, permission decisions, queue mutation, retry/cancel/send behavior | `mobile_dashboard.load_state` facade |
| `mobile_openclaw_bridge/mobile_prompt_contract.py` | Mobile delegation prompt and marker contract: batch ids, ack/result marker codes, marker stripping, output contract lines, supplement contract lines, permission context embedding, and attachment prompt lines | Queue mutation, task routing, result polling/recovery, permission policy decisions, Weixin delivery, or MCP fallback execution | `mobile_openclaw_cli.task_prompt` facade plus protocol regression checks |
| `mobile_openclaw_bridge/codex_session_owned_result.py` | Bounded read-only recovery of exact non-placeholder `mobile_result` blocks from Codex rollout JSONL, including assistant/agent record filtering, duplicate-copy hashing, conflict fail-closed behavior with a bounded manual-review marker, and actual visible rollout/thread evidence | Queue mutation, static thread binding, task completion, retry policy, or Weixin sending | Audit with `mobile_openclaw_cli.py audit-owned-result-recovery --task-id <id>`; corrective apply is restricted to a pushed known-placeholder task and requires `recover-owned-result --task-id <id> --apply --confirm CORRECT-OWNED-RESULT --expected-sha256 <audit candidate_result_sha256>`. Sender-accepted correction receipts only finalize local state; an intent without a receipt requires manual review and is never auto-resent. Validate with `codex_session_owned_result_tests.py`, `owned_result_correction_tests.py`, `session_store_owned_result_fallback_check`, ownership/recovery checks, and sender idempotency checks |
| `mobile_openclaw_bridge/codex_cdp_route_process.py` | Hidden asynchronous submission of the existing governed Codex Desktop launcher for visible-CDP recovery. It preserves the requested CDP port and returns a process receipt without imposing a shorter caller deadline on startup maintenance. | CDP probing, process killing, waiting for Desktop readiness, retry policy, task delivery, or replacing the launcher singleton | Consumed by `codex_cdp_route.ensure_codex_cdp`; validate with `python -m unittest _bridge\mobile_openclaw_bridge\codex_cdp_route_process_tests.py` and CDP route regressions |
| `mobile_openclaw_bridge/mobile_maintenance_probe_policy.py` | Read-only maintenance probe policy primitives: quick/deep skipped payloads, deep-probe allowlist parsing, probe enablement checks, and compact timing wrappers | Running probes, reading queues/databases/processes, deciding issue actionability, repair planning, state mutation, or message delivery | consumed by `mobile_maintenance.inspect_system`; validate through maintenance summary/doctor and `code_maintainability.py validate` |
| `mobile_openclaw_bridge/mobile_mcp_fallback_client.py` | Fresh local stdio calls into the mobile MCP server for supplement `get_pending_batch` / `ack_message` fallback when the active Codex MCP transport is unavailable | MCP tool implementation, supplement business semantics, queue mutation outside the MCP server, native MCP replacement when current-turn callable | `mobile_openclaw_cli` fallback facades and `supplement-cli-fallback-check` |
| `mobile_openclaw_bridge/delivery_runtime_state.py` | Delivery retry and route recovery runtime state: retry cooldown policy, delivery retry markers, thread recovery markers, active route lease expiry, and diagnostic event coalescing | Dispatch execution, final reply sending, permission decisions, owned-result recovery, app-server repair execution, or task completion | `mobile_openclaw_cli` worker dispatch/recovery paths and retry/recovery regression checks |
| `mobile_openclaw_bridge/enqueue_command_cli.py` | OpenClaw inbound `enqueue` CLI command flow: control-message shortcut, capability passphrase reply handling, permission wait/reject branches, context-token attachment, queue enqueue, and immediate received/confirmation acknowledgements | Worker dispatch, final reply generation, permission policy decisions, attachment materialization internals, or queue schema ownership | `mobile_openclaw_cli.main` facade and enqueue/control/capability regression checks |
| `mobile_openclaw_bridge/mcp_dispatch_gate.py` | Read-only current-session MCP gate decision before hidden Codex app-server dispatch creates another turn | MCP repair, process cleanup, retry scheduling, queue mutation, or delivery execution | `mobile_openclaw_cli` worker dispatch path and route/gate regression checks |
| `mobile_openclaw_bridge/thread_prewarm_state.py` | Codex app-server thread prewarm runtime marker helpers: cooldown/budget config readers, `thread_prewarm:*` marker read/write, and prewarm scheduling audit event | App-server probing, background process startup, dispatch decisions, retry scheduling, or task queue lifecycle | `mobile_openclaw_cli` worker dispatch path and thread prewarm regression checks |
| `mobile_openclaw_bridge/thread_route_state.py` | Thread route lookup and runtime-key helpers: configured thread list, active-thread selection, pending thread-selection TTL state, continuation keys, and basic dispatch runtime key names | Dispatch execution, retry timing policy, result recovery, permission decisions, queue schema ownership | `mobile_openclaw_cli` route facades and route/fairness regression checks |
| `mobile_openclaw_bridge/worker_active_recovery.py` | Worker active-task recovery sub-stages: stale `queued_for_codex` recovery, queued materialized-turn rehydration, same-route active-owner protection, and queued-only recovery result shaping | Sent/processing result polling, final reply delivery, supplement ownership, permission decisions, app-server repair execution, or worker dispatch selection | `mobile_openclaw_cli.recover_active_codex_tasks` facade and active recovery / rehydrate / route fairness regression checks |
| `mobile_openclaw_bridge/worker_dispatch_permission.py` | Worker dispatch permission gate: per-task ask-scope enforcement, capability passphrase wait handling, wait-conflict rejection, ordinary ask rejection, and status acknowledgements before dispatch | Permission policy calculation, token/grant storage, enqueue-time permission handling, Codex dispatch, queue schema ownership, or final replies | `mobile_openclaw_cli.worker_once` dispatch scan plus capability/passphrase and permission prompt regression checks |
| `encoding_governance.py` | UTF-8 and mojibake diagnosis/repair planning | Semantic correctness of decoded content | `doctor`, `repair-plan`, `validate` |
| `shared/record_store_maintenance.py` | Record/index growth, queryability, and persistence of derived governance tables across index rebuilds | Business task completion semantics or direct domain repair | `snapshot`, `doctor`, `repair-plan`, `index --apply`, `validate`, `metrics` |
| `resource_library_catalog.py` | Machine-readable resource-library module manifest and generated human README with owner, purpose, authority, and maintenance entrypoints | Mail, scheduler, memory, backup, record, resource-download, or website business state | `snapshot`, `doctor`, `repair-plan`, `build --apply`, `validate` |
| `shared/codex_scheduler_runner.py` | Unified maintenance wake loop, due evaluation, execution records, bounded heartbeat/metrics, retry budgets, and reconciliation between default task declarations, explicit overrides, and the materialized runtime table | Domain business state, arbitrary background commands, implicit approval of mutating tasks, or storing full owner stdout in heartbeat/status | `snapshot`, `metrics`, `task-drift`, `override-plan`, `migrate-overrides --apply --confirm MIGRATE-SCHEDULER-OVERRIDES`, `doctor`, `repair-plan`, `validate`, `run-due`, `loop` |
| `persistent_task_kernel.py` / `persistent_task_kernel_tests.py` | Sidecar durable task control state: idempotent enqueue, lease/ack/execution transitions, approval pause, bounded retry, dead-letter, expired-lease classification, receipts, and isolated behavior regression. It is intentionally not registered into the unified maintenance wake loop in phase 1. | Executing owner commands, sending mail, creating Codex threads, replacing owner business state, auto-starting a worker, or changing existing scheduler/email/mobile routing. | `snapshot`, `doctor`, `repair-plan`, `validate`, `metrics`, `behavior-eval`, `recover-expired --apply --confirm RECOVER-EXPIRED-TASKS`; validate with `python _bridge\persistent_task_kernel_tests.py` |
| `shared/migration_ledger.py` | Immutable migration plans plus append-only applied/verified/rollback events in the existing record-store database | Moving files, changing domain state, or replacing owner-specific apply commands | `snapshot`, `validate`; domain owners call `create_operation` and `append_event` |
| `shared/incident_index.py` + `shared/codex_reporter.py` | Cross-day incident families, report occurrences, unique maintenance-run denominator, and publishable failure-rate metrics | Report generation, retries, process repair, or treating unresolved historical rows as denominator evidence | `codex_reporter.py incident-rebuild [--apply]`, `incident-metrics [--kind ...]`, `metrics` |
| `shared/email_state_index.py` | Derived email query index and stable-ID reconciliation across task/run/content/outbox/inbound/RFC Message-ID/SMTP receipt identities | Sending, deleting, resending, or direct business-state repair through SQLite | `email_scheduler.py state-index refresh --apply`, `state-index repair-plan`, `state-query --table reconciliation`, `validate` |
| `shared/scheduler_bridge_governance.py` | Explicit retired/active state for the legacy resource-library scheduler bridge and ledger-backed reversible archival | Current scheduling, email dispatch, resource jobs, or workflow execution | `snapshot`, `doctor`, `repair-plan`, `retire --apply --confirm RETIRE-SCHEDULER-BRIDGE`, `validate` |
| `iteration_layer_review.py` | Read-only controlled iteration findings, promotion candidates, validation plans, approval blocks, and closeout-display obligations | Applying proposals, editing skills/memory/project knowledge, changing bridge state | `--dry-run`, `--json --run-validation --validation-profile quick` |

## Selection Guide

- Unknown or cross-domain task: start with `workflow_orchestrator.py plan`.
- Need to audit global mechanism redundancy, contradiction, ownership drift, or
  cross-layer proposal/closeout/tool-routing fragmentation: use
  `global_coherence_doctor.py doctor|validate`. It aggregates existing
  read-only owners and does not repair or grant authority.
- Adding or changing a system member, MCP route, Hub adapter, resource owner
  route, startup-affecting tool, or architecture surface: use
  `system_membership.py plan|impact|upgrade-plan|validate` before closeout so
  required synchronized surfaces and the member's exit strategy are explicit.
- Retiring or removing a system member: use
  `system_membership.py retirement-plan|retirement-signal|repair-plan|validate`.
  Remove every active registration, generator, route, runtime, maintenance,
  guidance, dependency, and authority trace through its owning surface. Keep
  only the startup baseline's minimal machine-readable negative tombstone.
  Historical backups, checkpoints, archives, and migration sources may remain
  as isolated evidence, but they must never route, generate, recommend, repair,
  or validate the retired member as current.
- Planning non-trivial maintenance or upgrade work for an existing system: use
  `maintenance_upgrade_governance.py plan` to produce task-specific batches and
  evidence needs. It points to existing tool policies; it does not define a new
  CodeGraph/SQLite/validator priority chain.
- Need online/external resources or direct generic web fallback: use
  `online_access_gate.py plan|check|exception|validate`. Generic web is valid
  only with resource-layer unavailable/route-exhaustion evidence, explicit
  user direct-web instruction, or an explicit structured flag proving that a
  higher-precedence platform instruction required generic web. The platform
  flag is never inferred from natural language.
- Need a compact machine-first entry package for actual Codex work: use `codex_workflow_entry.py plan|preflight|closeout`. It wraps existing owners and does not execute business actions. At closeout, use its package as the primary entry; work notes are one field inside that package, not a parallel closeout path.
- Need to promote lessons after broad system work: use `iteration_layer_review.py`
  or `mobile_openclaw_cli.py maintenance iteration` as the read-only proposal
  gate. Even after approval, promote only the specific verified rule or
  validation entrypoint, not raw recent-file metadata. Run the referenced
  validation command before writing and re-read the target after writing.
- Skill choice unclear, MySkills underused, or a skill trigger may be missing:
  use `skill_orchestrator.py plan`. It is a routing/evidence layer only. It may
  propose MySkills scenario assignments or new-skill gaps, but applying those
  proposals still goes through MySkills with explicit approval.
- Need to know whether memory should be loaded: use `codex_workflow_gate.py memory-preflight`.
- Codex Desktop restart/resume is slow or old conversations recover poorly: use
  `codex_session_store_doctor.py doctor|repair-plan` first. Treat oversized
  active JSONL transcripts as restore-performance risk and use compression only;
  storage maintenance must not create continuation tasks, move sessions, archive
  sessions, or delete sessions. Restart-boundary maintenance may run
  `auto-maintain --apply --boundary pre-launch` through the governed Desktop launcher;
  it skips while Codex is running, backs up before rewriting, preserves
  messages and existing compacted context blocks, and only compacts event/tool
  output/reasoning payload bloat. Its non-blocking owner lock uses PID plus
  process-creation identity so an old PID-reused lock cannot suppress maintenance.
- Codex Desktop model choices or reasoning levels drift after CC Switch changes
  provider: use `codex_model_provider_watcher.py snapshot|validate|once`. The
  hidden `CodexModelProviderWatcher` task starts at logon and has an `IgnoreNew`-guarded
  five-minute recovery trigger as a fallback. Its supervisor immediately replaces
  a watcher child that exits with the implementation-change code, with a bounded
  anti-spin restart window. The two-second hot path uses file metadata probes;
  full implementation/source hashing and projection run only after a relevant
  input changes. The source signature includes the CC Switch provider ID,
  provider configuration fingerprint, and model-catalog declaration/hash; a
  provider change is reconciled only after two stable source polls. Unreadable
  sources, unresolved catalog provenance, and reconcile failures use bounded retry,
  and events retain compact decision evidence instead of complete source
  structures. Source changes are still detected on the two-second loop; the
  unchanged-state Desktop runtime-binding self-heal probe runs every ten seconds,
  with the five-minute scheduled trigger retained as an independent fallback.
  It watches `config.toml` and the active catalog plus the active
  Desktop AppServer module signature. Current Desktop
  builds are repaired at the real `list-models-for-host` AppServer request
  boundary and the local `models/list` React query is invalidated through the
  incoming `ipc-broadcast` path; the Electron bridge remains an old-build
  fallback. The same reconcile pass refreshes reasoning fields and Statsig
  state without editing provider configuration, then requests one Desktop page
  reload for a confirmed provider change. A failed hot refresh records
  `restart_required` as a terminal user-visible result instead of retrying in a
  loop; watcher implementation reloads use a separate internal restart signal
  and never imply that Codex itself must restart. Install or repair it through
  `install-codex-model-provider-watcher-task.ps1`; the elevated launcher also
  starts the same supervisor as a fallback. The governed launcher serializes with
  a named mutex and exclusive file-lock fallback, treats process-census failure as
  unknown rather than empty, and permits stale-process force cleanup only after
  explicit `CODEX_ALLOW_STALE_CODEX_CLEANUP=1` opt-in.
- Need PMB context for work: use Hub PMB tools first. The Hub may restart the configured idle-exited daemon once and retry the same read-only call. Continue forward through the capability-matrix fallback chain only after Hub recovery fails; use `_bridge/local_pmb_memory.py pmb-prepare` or `pmb-recall` only at the local CLI stage.
- Need code structure through CodeGraph: query with the exact file path and
  domain-specific symbols first. If CodeGraph returns a wrong area because the
  query used generic symbols, retry with tighter anchors before treating the
  index as unhealthy. Do not use CodeGraph as the first choice for broad
  Markdown/config/rule lookups; use `rg` with generated-tree exclusions. If a
  prior CodeGraph result reported auto-sync disabled, verify current state with
  protocol smoke or the project-local CLI before carrying that diagnosis
  forward; a later smoke showing watcher active supersedes the old observation
  for current work. If native CodeGraph is not current-turn callable, use Hub
  `codegraph_explore` before dropping to local CLI or `rg`.
- Need indexed state or record evidence: use SQLite MCP/Hub query first for
  `.sqlite`/`.db`, record-store, email-state, scheduler, queue, receipt, or
  database-backed evidence. Use raw file scans only after a bounded SQLite
  query is unavailable, insufficient, or points to a specific source path.
- Native MCP seems unstable: use `mcp_session_doctor.py validate` before Hub fallback.
- Need tool route selection data for Codex internal planning: use `mcp_capability_routes.py build|lookup|validate`; the generated JSON is machine-first and derived from the Markdown matrix.
- Need to acquire or classify a resource without hand-building tool calls: use
  `resource_cli.py request` as the Codex-facing broker entry. It may execute
  local safe `resource_cli` fetch/probe/materialize paths and returns a
  receipt plus a persisted manifest under the resource store. If the owner is
  an MCP/browser/domain/package-manager tool, treat `handoff_required` as an
  internal intermediate state for the same resource need: continue through the
  resource layer, call the requested owner tool or resource-layer-selected web
  route under its own permission boundary, then attach the result to the same
  request with `resource.attach_result` or `resource_cli.py attach-result`.
  Do not treat probe-only metadata as fetched content, and do not start an
  independent replacement fetch while `same_need_fetch_allowed=false`.
- Many MCP/resource processes exist: use `resource_process_doctor.py doctor`.
- Frequent blue PowerShell or black console windows appear: use `popup_window_doctor.py snapshot|observe` to classify Codex shell, MCP descendant, scheduled-task, workspace-service, and unknown sources before changing startup policy.
- Need an agent-native CLI for a GUI app, desktop app, codebase, or workflow: use the installed `cli-anything` skill and `_bridge/cli_anything_governance.py search|info|validate`. `cli-hub` discovery is trusted; installing a concrete harness still needs explicit task intent and post-install validation.
- Need to absorb memory/knowledge project ideas: use the existing local memory
  governance stack first. Treat `agent-memory-engine`, `ArcRift`, and
  `localmem` as references for local-first, evidence-backed, lifecycle-aware
  memory patterns, then route any durable change through
  `memory_governance.py` and `_bridge/external_knowledge.py`.
- Need base development tools for code work: first check
  `code_maintainability.py toolchain`, then use `rg`, `fd`, `uv`, `uvx`,
  `ruff`, or Playwright as utility accelerators for the owning workflow. They
  are not independent permission or validation authority.
- New edit backup or scattered backup cleanup: use backup router first, then `backup_hygiene_doctor.py`.
- One-shot current-task reminders: use `memory_governance.py work-note-add/read/clear`; after the main task, Codex first reads `codex_workflow_entry.py closeout`, then processes the package's work-note entries as a follow-up queue before final reply. Processing is automatic only for read-only analysis, inspection, validation, planning, and proposal drafting. Work-note-derived writes or external actions need separate explicit approval because the main task's authorization does not carry over. Good candidates include non-blocking tool routing observations, wrong-area index/search results, expected MCP namespace misses, fallback-use reasons, and stale matrix/rule hints discovered while the main task continues.
- Draft artifacts: store them in `_bridge/shared/drafts/` with explicit `Content maturity`, `Workflow status`, and `Pending action` fields. The directory is not a queue and is never a closeout trigger. Pending review uses the persistent `_bridge/workflow_review_queue.py` owner with an `artifact_ref`; closeout renders only `pending` rows, and `dispose` records the approved/revised/rejected/resolved/deferred/discarded outcome so handled cards do not repeat. `retained_reference` uses `Pending action: none`. Validate with both `draft_governance.py validate` and `workflow_review_queue.py validate`.
- Note, PMB, or memory cleanup: use `memory_governance.py` plans; apply note absorption only through explicit approved ids with `apply-approved --confirm-apply`; apply PMB fact cleanup only through `pmb-fact-repair-plan` followed by `pmb-fact-apply-approved --confirm-apply`, which writes review markers and does not delete or rewrite PMB events. After absorption, use `recall-checks` to keep retrieval verification visible and `recall-verify` for read-only local index/PMB evidence.
- Adding or repairing a slash template: use `slash_command_governance.py snapshot|proposal|validate|apply --confirm-apply|render-smoke`. For slash/scratch coordination metadata issues, use `tool_coordination.py validate`.
- Need to improve code maintainability or choose a refactor target: use `code_maintainability.py snapshot|module-context|module-assets|lookup-module|plan|validate` first and apply `_bridge/docs/code_maintainability_guidelines.md` for naming, structure, error handling, validation, and refactor boundaries. Use `module-assets --task-mode maintenance` for system maintenance/governance/owner-boundary work, and `module-assets --task-mode code` for implementation/reuse/scenario-fit work. Build `_bridge/runtime/module_capability_index.json` and `_bridge/runtime/module_asset_catalog.json` when missing or stale; both are derived caches, not source of truth. Apply focused incremental refactors separately with backups and validators.

## Memory Destination Matrix

Use this matrix when deciding where a captured observation should go. The
default for uncertain items is no long-term write.

| Input | Destination | Required Before Promotion | Verification |
| --- | --- | --- | --- |
| User preference or profile fact | user profile | explicit or repeated user signal, non-secret, not one-off task state | user-profile guidance selects it when relevant |
| Operational lesson or root cause | PMB or absorption index | current evidence, reusable behavior, incident details compressed | `recall-checks` plus `memory_governance.py validate` |
| Codex behavior constraint | global or workspace `AGENTS.md` | correct scope, no duplicate rule, mirror sync | `agents_rule_mirror.py validate` |
| Reusable procedure | skill or custom command proposal | repeatable inputs/outputs/boundaries | template render or skill gate dry-run |
| Skill routing observation | skill usage ledger or skill proposal | planned-vs-actual skill evidence, no sensitive task content | `skill_orchestrator.py usage-summary` plus targeted `SKILL.md` readback |
| Tool capability or fallback fact | MCP capability matrix or owning maintenance surface | current tool evidence, permission boundary preserved | tool matrix read-back and relevant doctor |
| External source knowledge | external knowledge evidence layer first | official/primary, scoped, reusable, non-sensitive | `external_knowledge.py doctor`, then distill plan |
| Current-task side issue | one-shot work note | valuable but non-blocking, safe to defer until main task completion; not an authorization grant | closeout reads raw note, processes read-only follow-up work, proposes any write separately, then clears it |

## Anti-Sprawl Checks

Before adding a new doctor or governance script, answer yes to all:

1. No existing surface owns this state.
2. The new surface will not duplicate another surface's diagnosis.
3. It has clear `snapshot`/`doctor`/`repair-plan`/`validate` or a deliberate smaller equivalent.
4. It is read-only by default.
5. It has a bounded validation command.

If any answer is no, extend the existing owning surface instead.
