# scheduler Maintenance Surfaces

Authority: maintenance contracts for this system. Load this shard only
after the maintenance registry selects the system. The compact index at
`../maintenance_surface_map.md` is navigation only and does not duplicate
the contracts below.

| Surface | Owns | Does Not Own | Usual Entry |
| --- | --- | --- | --- |
| `windows_kernel_pool_diagnostics.py` with `windows_kernel_pool_governance.py` and `windows_kernel_pool_remediation.py` | Microsoft-signed PoolMon capture, compact success summaries, failure evidence retention, boot-segmented SQLite trends, bounded periodic monitoring, WPN recovery guards, and fingerprint-locked reversible stale local-firewall-rule cleanup | Unconditionally restarting services, changing drivers, disabling firewall/notifications, broad firewall cleanup, changing page-file or memory policy, deleting failure evidence, treating aggregate Pool Tags as unique driver attribution, or combining built-in GPU and Pool WPR profiles in unbounded file mode | `capture --label <label> [--summary-only]`, `index-rebuild`, `doctor`, `metrics`, `schedule-plan`, guarded `schedule-apply --confirm INSTALL-KERNEL-POOL-MONITOR`, `wfp-plan`, fingerprint-locked `wfp-apply`, governed `wfp-rollback`, `wpn-plan`, guarded `wpn-recover --confirm RESTART-WPN-HANDLE-LEAK`, and `validate`; when ETW is necessary use one memory-mode/custom-buffer profile with an explicit duration and output-size budget; validate with the three `windows_kernel_pool_*_tests.py` suites and `code_maintainability.py validate` |
| `performance_doctor.py` + `shared/performance_maintenance_job.py` | Read-only cross-platform workstation performance sampling and the bounded scheduled maintenance composition. The job dispatches queue-independent owner commands before any mobile queue initialization, reuses one existing Windows-host bridge receipt for queue guard and app-server decisions, and propagates host-probe failure instead of reporting false success. | Changing performance policy, terminating processes, applying unapproved remediation, opening the Windows mobile WAL database from WSL, repeating the same bridge probe in one run, persisting full command-line process dumps, bypassing the Windows execution boundary, or treating a single sample as a capacity conclusion | `snapshot`, `doctor`, `repair-plan`, `metrics`, `validate`; scheduled `performance_quick_metrics` and `performance_auto_maintenance_30min`; validate with `performance_doctor_tests.py`, `shared/performance_maintenance_job_tests.py`, `mobile_openclaw_bridge/mobile_cli_queue_affinity_tests.py`, and unified scheduler `task-drift` |
| `maintenance_scheduler_service.py` + `shared/codex_scheduler_runner.py` + `shared/scheduler_task_recovery.py` + `shared/install-codex-scheduler-task.ps1` | The only WSL maintenance loop and declared due-task authority. Alongside existing owner tasks it invokes exactly one hourly `maintenance_convergence_shadow_scan`, which may only generate a bounded derived plan artifact from coverage/signals. Scheduler validation rejects duplicate dispatchers and any static direct invocation that bypasses the convergence runtime. The existing approved-review consumer remains a fallback after immediate handoff. Mutable task state, lock, heartbeat, records, governance and logs remain under WSL XDG state; guarded recovery reuses the registered task's normal command, receipt and transition. Windows remains a limited one-shot login wake, not a resident loop | Domain business state, arbitrary commands, inferred approval, executing owner repairs from the shadow task, rule/skill/config/business-state application, a second scheduler database or loop, static and dynamic double execution, mirror-output feedback into source maintenance, clearing retry-exhausted state without a fresh owner attempt, automatic `/mnt` legacy reads, keeping periodic execution resident in Windows, or treating task start/plan creation as business completion | Service `plan|status|validate|install --confirm INSTALL-CODEX-MAINTENANCE-SCHEDULER|windows-wake-plan|install-windows-wake --confirm INSTALL-CODEX-WSL-CONTROL-PLANE-WAKE`; runner `snapshot|metrics|task-drift|doctor|repair-plan|validate|run-due|recover-task ...|loop`; shadow owner `maintenance_convergence_runtime.py shadow-scan|validate` |
| `shared/scheduler_bridge_governance.py` | Explicit retired/active state for the legacy resource-library scheduler bridge and ledger-backed reversible archival | Current scheduling, email dispatch, resource jobs, or workflow execution | `snapshot`, `doctor`, `repair-plan`, `retire --apply --confirm RETIRE-SCHEDULER-BRIDGE`, `validate` |
