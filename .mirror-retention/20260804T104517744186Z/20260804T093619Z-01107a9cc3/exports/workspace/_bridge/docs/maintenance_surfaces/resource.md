# resource Maintenance Surfaces

Authority: maintenance contracts for this system. Load this shard only
after the maintenance registry selects the system. The compact index at
`../maintenance_surface_map.md` is navigation only and does not duplicate
the contracts below.

| Surface | Owns | Does Not Own | Usual Entry |
| --- | --- | --- | --- |
| `shared/windows_runtime_assets.py` + `mobile_openclaw_bridge/retire-openclaw-legacy-runtime.ps1` | Single Windows path authority for OCR, OpenClaw, and the reusable pip cache. OpenClaw runs from `%LOCALAPPDATA%\Codex\openclaw`; retirement removes the legacy project-tree duplicate and fixed install-only caches only after the managed gateway process and port are healthy. The OCR `pip-cache` path projects the single `%LOCALAPPDATA%\pip\cache` authority so wheel downloads remain reusable. | Deleting pip/wheel caches, account state, secrets, Node, the OpenClaw package, active logs, arbitrary legacy roots, reparse targets, or running without the exact confirmation | Plan with `retire-openclaw-legacy-runtime.ps1`; apply only in the existing approved-elevated scheduler lane with `-Apply -Confirm RETIRE-LEGACY-OPENCLAW-RUNTIME`; validate port 18789, managed process paths, host projection freshness, and cache junction target |
| `online_access_gate.py` | Read-only direct-web access gate for online/external resource needs. It emits the resource-layer-first plan and validates that generic web is used only after resource-layer unavailability, configured owner/Hub route-chain exhaustion evidence, or explicit user direct-web instruction | Executing web/resource requests, hiding platform tools, mutating resource-layer state, replacing source-owning MCPs, or approving broad fallback without evidence | `plan`, `check`, `exception`, `validate` |
| `codex_desktop_model_runtime.py` + `restart-codex-desktop-cdp.ps1` | CDP-backed model-runtime inspection and process-preserving Desktop page refresh. The Python owner selects the Codex page and issues `Page.reload`; the projected PowerShell facade supplies hidden Windows execution, timeout, and same-process/CDP acceptance | Closing or force-stopping the Desktop GUI, launching a replacement Desktop process, patching the signed package, or treating a reload request alone as completion without process/CDP readback | `codex_desktop_model_runtime.py page-reload --wait-seconds <seconds> --json`; projected facade `restart-codex-desktop-cdp.ps1`; regression via `codex_model_provider_tests.py`, `codex_startup_chain_tests.py`, and `wsl_workspace_owner_tests.py` |
| `codex_desktop_environment_selection.py` + `wsl_codex_runtime.py` | Bidirectional Desktop environment selection reconciliation across the Windows host and isolated WSL app-server config, using last-synchronized state, routed backups, optimistic source checks, a single-writer lock, atomic writes, rollback, and full `[desktop]` table preservation before mode-specific MCP/session projection. The WSL runtime also exposes focused `hooks-plan|hooks-apply|hooks-validate`, `plugins-plan|plugins-apply|plugins-validate`, and `skills-plan|skills-apply|skills-validate` entrypoints; each avoids unrelated config/session/state/plugin writes, while the skills path plans both the runtime and canonical user roots before adding any missing Work Git symlink so a conflict blocks all targets before mutation. The WSL runtime registers only the session-bound node_repl wrapper and local Hub, with Hub-managed MCPs consumed through the Hub catalog instead of duplicated registrations; it replaces plugin, marketplace, shell, project, and other platform roots from its template while retaining only explicitly allowlisted portable user roots such as `personality`. Its node_repl wrapper projects Linux `sandboxCwd` file URIs to Windows-local drive or WSL UNC URIs before the Windows server consumes them, without changing tool arguments or permission profiles. `codex_state_repair.py` reads environment selection from the Windows host config and delegates these WSL roots instead of rewriting them during fallback | Sharing the complete host config with WSL, duplicating Hub-managed profiles as eager MCP registrations, selecting a provider/model, weakening WSL isolation, coupling a focused hook/plugin/skill change to ambient session or state drift, patching the signed Desktop package, blocking Codex startup after an owner failure, treating Windows fallback as authority to rewrite WSL commands, preserving arbitrary unknown roots, changing node_repl permission profiles, or treating projection success as proof that the restarted UI consumed it | `codex_desktop_environment_selection.py plan\|apply\|validate`; `wsl_codex_runtime.py plan\|apply\|validate`, focused `hooks-plan\|hooks-apply\|hooks-validate`, focused `plugins-plan\|plugins-apply\|plugins-validate`, or focused `skills-plan\|skills-apply\|skills-validate`; real node_repl initialize/list/call smoke via `wsl_codex_runtime_tests.py`; automatic first-stage consumption by `codex_config_guard.py run-once --apply --phase pre-start-static`; regressions in `codex_desktop_environment_selection_tests.py`, `wsl_codex_runtime_projection_tests.py`, `wsl_codex_runtime_hooks_tests.py`, `codex_state_repair_tests.py`, and `codex_startup_chain_tests.py` |
| `tool_exposure_doctor.py` | Read-only separation of Codex MCP config, CLI visibility, Hub-managed registration, local process evidence, current-turn tool callability, and native Codex App tool discovery; absent persisted dynamic-tool metadata is a schema-discovery gate, never proof that a native App tool is unavailable | MCP execution, config mutation, credential changes, process cleanup, inventing a static App-tool catalog, or claiming current-turn usability/unavailability without fresh schema or call evidence | `snapshot`, `doctor`, `repair-plan`, `validate`, `metrics`; validate with `tool_exposure_doctor_tests.py` and `resource_process_doctor.py validate` |
| `local_mcp_hub_resource_search.py` | Read-only bounded text, image, news, video, book, and explicit-URL extraction tools for the resource layer's `generic_search` owner; consumes `managed_python_dependency_runtime.py` for ABI/platform-scoped DDGS selection and fresh-process `ddgs` plus `lxml.etree` import acceptance; one caller total timeout is divided across selected backends and the outer worker retains only a fixed exit grace; retains every attempted query/backend and emits explicit terminal no-results evidence after the configured chain is exhausted | Resource intent/routing, downloads, installs, persistent writes, global proxy/DNS changes, direct-Web permission decisions, per-phase timeout resets, or maintaining a second dependency-path/ABI authority | Hub `resource_search.health|validate|text|images|news|videos|books|extract`; `python _bridge\local_mcp_hub.py validate`; `python _bridge\local_mcp_hub_resource_search_tests.py` |
| `clash_mihomo_control.py` / `clash_node_metrics.py` | Secret-free multi-profile Clash Verge discovery, current-profile/config/group identity, separate proxy-listener and node-control health, signature-invalidated node evidence cache, bounded delay sampling, isolated real-access probes, and request-aware recommendation | Editing subscriptions, switching the current profile or main node, changing system proxy/DNS, treating control-plane degradation as proof that the business proxy is down, or probing non-current profiles outside an isolated plan | `clash_mihomo_control.py validate`; `clash_node_metrics.py snapshot|recommend|validate`; regression via `clash_resource_adaptive_tests.py` |
| `resource_request_admission.py` / `resource_broker.py` / `resource_store.py` / `resource_scheduler.py` | Admission-time distinction among one URL, URL lists, discovery queries, local paths, and packages; candidate-only entity alias normalization; semantic rejection of incompatible documentation intent for entity/platform discovery; multi-URL promotion to the existing batch owner; single-request orchestration, safe read-only owner continuation under one request id, forward-only fallback, URL-only probe exclusion when no URL exists, structured batch-item identity, independent acceptance, bounded concurrency, handoff tracking, owner result attachment, retry evidence, terminal route-chain no-results receipts, persisted manifests, and receipts for Codex consumption | Bypassing MCP permissions, silently rewriting an entity name, merging independent targets into one natural-language query, treating handoff or command success as resource acceptance, destructive installs without risk judgement, browser/GitHub remote writes, deciding proxy/node policy, or durable permission changes | `request`, `request-batch`, `status`, `attach-result`; via `resource_cli.py request|request-batch|batch-status|status|attach-result` or Hub `resource.request|resource.request_batch|resource.status|resource.attach_result`; validate admission/entity discovery with `resource_request_admission_tests.py`, search exhaustion with `local_mcp_hub_resource_search_tests.py`, and end-to-end behavior with `resource_fetcher_tests.py` |
| `resource_execution_budget.py` with broker, owner, source, and scheduler consumers | One monotonic total deadline for each resource request or batch, remaining-time propagation to downstream phases, control-plane execution reserve, and terminal `total_budget_exhausted` evidence for work that must not start | Per-phase timeout resets, unlimited retries, network-route policy, silently raising caller budgets, or treating timeout exhaustion as a successful empty result | `python _bridge\resource_execution_budget.py`; behavioral coverage in `python _bridge\resource_fetcher_tests.py` |
| `resource_owner_tool_registry.py` / `resource_owner_executor.py` / `resource_package_owner.py` / package adapters | Canonical owner names and bounded execution. Python packages support `inspect_then_install` for zero-write official metadata, lock, hash, ABI and permission planning, and `verified_auto_install` for one-request validation, hash-checked isolated installation, fresh import and acceptance. Write mode is `approved_write`, never uses read-only caches, and consumes scoped authorization bound to the lock, ABI target, current turn/run and idempotent operation. | Route ordering, permission expansion, global installs, implicit approval, treating `install_approved` as authorization, or sharing write results through read-only caches | `python _bridge\resource_owner_executor.py validate`; `python _bridge\resource_package_owner_tests.py`; `python _bridge\resource_python_package_installer_tests.py`; full coverage in `resource_fetcher_tests.py` |
| `structured_task_envelope.py` / `resource_cli.py custom` | Versioned machine-readable resource delegation for action, target, kind, quantity, uniqueness, source coverage, freshness, transfer, package, materialization, quality, safety, and source execution. `resource.execution` carries operations, selectors, deliverables, ordered phases, limits, output depth, and acceptance; required phases/deliverables block metadata-only false completion. CLI and Hub/MCP callers share the existing broker/job/receipt implementation | A second queue or retry engine, natural-language permission inference, silently completing unsupported phases, owner-tool permission expansion, network route selection, or replacing legacy commands before compatibility validation | `python _bridge\structured_task_envelope.py`; `python _bridge\structured_task_envelope_tests.py`; `python _bridge\resource_cli.py custom --request-json <json> --json`; complex phase regressions in `resource_fetcher_tests.py` |
| `resource_acceptance.py` / `resource_strategy_shadow.py` / `resource_candidate_quality.py` / `shared/resource_event_store.py` | Canonical business acceptance, read-only source-lane shadow planning, explainable candidate vectors, and the existing SQLite projection for signatures, consumption, satisfaction, duplication, and lane metrics | A second broker, queue, retry state machine, live route-order mutation in phase A, permission changes, or metadata/HTTP success standing in for requested content or artifacts | Focused acceptance, shadow, and event-store tests; full broker regression in `resource_fetcher_tests.py` |
| `resource_transfer_contract.py` / `resource_transfer_owner.py` / `resource_transfer_worker.py` | One durable direct-file transfer identity and state machine in the existing resource event store; verified Range resume, disk/hash checks, atomic placement, scoped-authorization generation fencing, submit-once long-command receipt consumption, and wake-only reconciliation by the unified scheduler. Dynamic source ordering is enabled only for fixed allowlisted task classes and cannot introduce an owner | Source discovery, a second broker/database/scheduler/retry loop, arbitrary URL or media download, permission inference, profile/proxy/DNS changes, aria2 RPC, or treating state persistence as business success | Local Range fixtures and lifecycle coverage in `resource_transfer_tests.py`; strategy allowlist in `resource_strategy_shadow_tests.py`; owner `status|reconcile`; full resource and scheduler regressions before activation |
| `resource_download_backends.py` | Optional process-backed URL materialization plus platform-aware discovery evidence for WSL/Windows curl, aria2c, yt-dlp, and ffmpeg; separates installed, callable, platform, path, and validation evidence | Installing tools, running aria2 RPC daemons, changing system proxy/DNS, selecting global routes, or bypassing resource policy/permissions | `validate`; consumed by `resource_fetcher.py` after policy selection |
| `resource_windows_package_manager.py` | Windows package-manager package owner adapter: Chocolatey/winget availability, bounded search, install planning, explicitly approved install execution, and verify command metadata for resource package requests | Silent installs, uninstall/upgrade workflows, global proxy/DNS or package-manager configuration changes, permission escalation, or replacing package policy/risk review | `availability`, `validate`; consumed by `resource_package_owner.py` for `windows_tool`/`choco`/`winget` ecosystems |
| `managed_python_dependency_runtime.py` / `policies/managed_python_dependencies.lock.json` / `resource_python_package_installer.py` | Single authority for runtime identity, exact artifact URLs and hashes, ABI/platform targets, stable lock signatures and fresh-process import acceptance. The installer consumes scoped authorization before its first side effect, stages hash-checked requirements, records effect readback, atomically replaces the target and reuses a valid target without rerunning pip. | Global/user-site installs, version discovery authority, PATH/pip mutation, implicit approval, authorization by boolean, or accepting distribution metadata without import evidence | `python _bridge\managed_python_dependency_runtime.py validate`; `select --dependency-root <runtime_dependencies> --package <name>`; focused package tests |
| `resource_content_store.py` | One-response full raw/decoded content authority, bounded preview, hash/index references, offline line/byte/query expansion, and managed-BeautifulSoup-first HTML parsing with explicit stdlib fallback. | Network acquisition, a second request store, global Python imports, re-fetch on expansion, or treating preview truncation as completion | `python _bridge\resource_content_store_tests.py`; end-to-end coverage in `resource_fetcher_tests.py` |
| `cloakbrowser_owner.py` | Explicit-use CloakBrowser planning and diagnostics, isolated wrapper discovery, and separate browser-binary acquisition requirements with provenance and hash evidence | Replacing normal Chrome/Playwright/DevTools routes, implicit patched-browser download, normal profile/cookie reuse, permission inference, or global proxy mutation | `snapshot`, `doctor`, `plan --task <task> [--authorized]`, `validate` |
| `resource_network_execution.py` | Resource-layer adapter from `codex_network_gateway.py` plans to executable env/unset packages, URL attempt specs, and owner-tool handoff network guidance | Network policy decisions, resource semantics, starting live leases, mutating system proxy/DNS, bypassing owner MCP permissions, or executing package/GitHub/docs/browser tools | `validate`; consumed by `resource_fetcher.py` and `resource_broker.py` |
| `resource_process_doctor.py` with `resource_process_reporting.py` | Resource/MCP process fanout, stale launch batches, current-session evidence mapping, bounded diagnostic/cleanup receipts, and safe owner cleanup. Observed totals remain visible, while cleanup and memory-pressure issues use age-qualified persistent roots/process working set; short startup waves are recorded as transient and do not create cleanup review items. | Current-turn tool callability, bridge queues, ordinary application memory policy, fixed-PID cleanup, or treating a single launch wave as a persistent leak | `doctor`, `metrics`, `repair-plan`, `cleanup [--safe-apply --apply]`, `validate`; current coverage includes session-bound `desktop-weixin` and other declared MCP profiles; fanout defaults to a two-minute evidence gate before it becomes actionable |
| `resource_process_observations.py` | Read-only current-turn MCP observation log parsing, freshness filtering, and profile-to-process-group evidence mapping | Process snapshot collection, process cleanup, repair planning, permission decisions, or route fallback execution | consumed by `resource_process_doctor.py`; validate through `resource_process_doctor.py doctor\|metrics\|validate` |
| `cli_anything_governance.py` | CLI-Anything / cli-hub installation health, catalog discovery, local installed-harness discovery and required command surfaces, Codex skill completeness, trusted-project policy | Automatically installing harnesses, running target software during read-only validation, bypassing package-manager review, MCP configuration | `snapshot`, `doctor`, `validate`, `metrics`, `search`, `info`, `installed`, `commands` |
| `developer_toolchain_owner.py` | Version-locked WSL developer CLI lifecycle for required `rg`, `fd`, `uv`, `uvx`, `ruff`, native Linux `gh`, and `jq`; consumes resource-layer package/download operations, uses a user-local managed root, hash-checks release archives and single-file binaries, rejects unsafe extraction and foreign PATH entries, projects managed symlinks atomically, and retains old versions for rollback. The `gh` wrapper borrows the existing Windows `gh.exe` keyring token only in process memory so WSL keeps native Git/path semantics without persisting a second credential; a guarded user-path shim exposes that managed wrapper to the non-interactive WSL app-server PATH | System-wide installs, bypassing resource acquisition, deleting old versions automatically, overwriting unrelated user executables, persisting copied host credentials, broad formatting, or replacing owning validators | `snapshot`, `plan`, `apply --confirm INSTALL-DEVELOPER-TOOLCHAIN`, `validate`; versions and sources in `policies/developer_toolchain.lock.json`; regression via `developer_toolchain_owner_tests.py` |
| `resource_library_catalog.py` | Machine-readable resource-library module manifest and generated human README with owner, purpose, authority, and maintenance entrypoints | Mail, scheduler, memory, backup, record, resource-download, or website business state | `snapshot`, `doctor`, `repair-plan`, `build --apply`, `validate` |
