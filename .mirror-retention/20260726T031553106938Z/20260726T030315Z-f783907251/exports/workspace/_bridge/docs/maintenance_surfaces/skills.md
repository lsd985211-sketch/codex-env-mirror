# skills Maintenance Surfaces

Authority: maintenance contracts for this system. Load this shard only
after the maintenance registry selects the system. The compact index at
`../maintenance_surface_map.md` is navigation only and does not duplicate
the contracts below.

| Surface | Owns | Does Not Own | Usual Entry |
| --- | --- | --- | --- |
| `skill_active_catalog.py` + `skill_admission_discover.py` + `skill_orchestrator.py` + `skill_lifecycle_governance.py` + `skill_lifecycle_state.py` | Available configured-plugin skill-payload indexing, authoritative admission/trust registry, stable skill identity across unambiguous moves, skill preflight, incremental user/system/plugin fingerprint refresh, persistent SQLite change/quality/lineage evidence, global-root/MySkills/plugin inventory fusion, runtime quarantine, collision review, scenario migration plans, approved archive actions, bounded default metadata manifests, and planned-vs-actual usage evidence. Missing local skill payloads are reported as deferred context; `codex_plugin_config_health.py` remains the sole authority for plugin installation and cache completeness. Batch validators refresh one routing context and reuse it for per-message scoring, while independent commands still refresh current state. | Plugin installation/update or cache-health decisions, a second approval authority in SQLite, automatic skill mutation/retirement, quality overriding domain or runtime eligibility, unapproved skill rewrites/deletion, plugin/system source mutation, task execution, stale cross-process routing caches, or bypassing MySkills for ordinary lifecycle writes | `skill_admission_discover.py`; `plan`, `refresh`, `lifecycle-state`, `audit-plan`, `repair-plan`, `record-lineage`, `apply-approved --confirm-apply`, `scenario-plan`, `record-usage`, `usage-summary`, `doctor`, `validate`; plugin health via `codex_plugin_config_health.py` |
| `code_maintainability.py` | Code maintainability metrics, refactor target planning, module-context routing, developer toolchain status, the derived module capability index at `_bridge/runtime/module_capability_index.json`, and the categorized module asset catalog at `_bridge/runtime/module_asset_catalog.json`. It helps Codex find reusable modules before creating new code and supports two lookup modes: maintenance tasks emphasize owning system, responsibility, state behavior, owner CLI, and validator; code tasks emphasize role, applicable scenario, reuse target, public entrypoints, and capability terms. `build-module-index --limit` limits only response samples and never truncates the persisted index. Local coding rules are documented in `_bridge/docs/code_maintainability_guidelines.md`. | Formatting, automatic refactors, semantic correctness, deleting backup files, applying refactors without separate approval, treating derived runtime indexes as source of truth, or using categories as permission boundaries | `snapshot`, `toolchain`, `module-context`, `module-assets --task-mode maintenance|code`, `build-module-index`, `lookup-module`, `plan`, `validate` |
